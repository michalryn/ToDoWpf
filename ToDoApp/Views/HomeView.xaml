<UserControl x:Class="ToDoApp.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid 
        Margin="8,0,10,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
                Background="{StaticResource PrimaryColor}"
                CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Black" Opacity="0.3"/>
            </Border.Effect>
            <TextBlock Padding="40,15,40,15" Text="Search" FontSize="16" Foreground="{StaticResource WhiteColor}"/>
        </Border>

        <TextBlock
            Grid.Row="1"
            Text="Tasks" 
            Foreground="{StaticResource ForegroundColor}" 
            FontSize="25"
            FontWeight="Bold"
            Margin="8"/>
        <StackPanel  
            Grid.Row="2"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <Button Content="Task"
                    Height="25"
                    FontWeight="Bold"
                    Command="{Binding Path=NavigateToTaskViewCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
