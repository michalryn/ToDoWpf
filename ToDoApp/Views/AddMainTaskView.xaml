<UserControl x:Class="ToDoApp.Views.AddMainTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8,0,10,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="150" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Border
            Grid.RowSpan="11"
            Grid.ColumnSpan="2"
            Background="{StaticResource WhiteColor}"
            CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Black" Opacity="0.3"/>
            </Border.Effect>
        </Border>
        <TextBlock 
            Grid.Row="0"
            Text="Add task"
            FontSize="18"
            FontWeight="Bold"
            Padding="8"
            Foreground="{StaticResource ForegroundColor}"/>
        <Label Grid.Row="1" Content="Title"/>
        <TextBox
            Grid.Row="2"
            Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
            />
        <Label Grid.Row="3" Content="Priority"/>
        <TextBox
             Grid.Row="4"
            Text="{Binding PriorityLevel, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Grid.Row="5" Content="Start date"/>
        <DatePicker Grid.Row="6" SelectedDate="{Binding StartDate}"/>
        <Label Grid.Row="5" Grid.Column="1" Content="Deadline date"/>
        <DatePicker Grid.Row="6" Grid.Column="1" SelectedDate="{Binding DeadlineDate}"/>
        <Label Grid.Row="7" Content="Description"/>
        <TextBox 
            Grid.Row="8"
            Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
        <Button 
            Grid.Row="9"
            Content="Add task" Command="{Binding AddMainTaskCommand}"/>
    </Grid>
</UserControl>
